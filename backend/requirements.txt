# SecureVision backend runtime dependencies.
#
# Install order (see HOW_TO_RUN.md):
#   1. PyTorch for your hardware (NOT listed here):
#        NVIDIA RTX 50xx / 40xx / 30xx:
#          pip install torch==2.7.1 torchvision==0.22.1 --index-url https://download.pytorch.org/whl/cu128
#        CPU only:
#          pip install torch==2.7.1 torchvision==0.22.1 --index-url https://download.pytorch.org/whl/cpu
#   2. pip install -r requirements.txt
#   3. pip install --no-deps -r requirements-nodeps.txt
#
# Face recognition uses facenet-pytorch (FACE_BACKEND=facenet). DeepFace is
# optional and NOT installed by default: switching backends invalidates every
# enrolled face, because embeddings from different models are not comparable.

Flask==3.1.2
waitress==3.0.2
flask-cors==6.0.1
itsdangerous==2.2.0   # signed session cookies (auth.py)
pymongo==4.15.4
numpy==2.2.6
opencv-python==4.12.0.88
pillow==11.3.0
scipy==1.16.3
ultralytics==8.3.230
timm==1.0.29
requests==2.32.5
tqdm>=4.66
psutil>=5.9
PyYAML>=6.0

# dependencies of the --no-deps packages in requirements-nodeps.txt
fvcore==0.1.5.post20221221
iopath==0.1.10
av==18.1.0
parameterized>=0.9
